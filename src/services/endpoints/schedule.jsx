import api from '../api';

const getCalendar = (month, token) => {
    //backend fetches user data
    //checks uses access control
    //get personal schedule based on month
    //get available departments and teams & overall statistics
}

const getTeamSchedule = (dept, team, startDate, endDate, token) => {
    //called each time a range is selected or a new department or team is selected on UI
    //fetch user data
    // checks access control
    // fetch WFH status based on 
}

export 
{
    getCalendar,
    getTeamSchedule
}